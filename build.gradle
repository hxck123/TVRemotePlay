buildscript {
    repositories {
        google()
        mavenCentral() // 替换jcenter()
        maven { url 'https://maven.google.com' }
    }
    dependencies {
        // Android plugin for gradle - 使用兼容的版本
        classpath 'com.android.tools.build:gradle:3.6.4'
    }
}
allprojects {
    repositories {
        google()
        mavenCentral() // 替换jcenter()
        maven { url 'https://maven.google.com' }
    }
}
task clean(type: Delete) {
    delete rootProject.buildDir
}
